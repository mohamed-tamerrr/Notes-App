import 'package:flutter/material.dart';
import 'package:p/features/notes/widgets/note_view_body.dart';

class Noteview extends StatelessWidget {
  const Noteview({super.key});

  @override
  Widget build(BuildContext context) {
    return NoteViewBody();
  }
}
